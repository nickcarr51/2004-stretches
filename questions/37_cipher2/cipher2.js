const decypher = (str) => {
  return str.split(' ').map(word => {
    let newStr = '';
    // let firstLetter = word.split('').filter(char => typeof parseInt(char, 10) === 'number');

    // console.log(firstLetter)
    // ahhhhh
  })
};
module.exports = { decypher };
